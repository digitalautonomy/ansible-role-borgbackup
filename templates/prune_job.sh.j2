#!/bin/sh

set -e

export BORG_RSH="{{ borgbackup_rsh_cmd }}"
export BORG_PASSPHRASE="{{ borgbackup_passphrase }}"

REPOSITORY={{ borgbackup_server_user }}@{{ borgbackup_client_backup_server }}:{{ item.name }}

{{ borgbackup_binary }} prune \
  {{ item.prune_options }} \
  $REPOSITORY
